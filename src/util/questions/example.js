const name = 'example'
const intro = 'Just showing how this works'
const code = `
function returnTrue() {
  // Return true and press alt+enter

}`
const tests = [
  { param: 'Doesn\'t matter in this case', result: true }
]

export default { name, intro, code, tests }
